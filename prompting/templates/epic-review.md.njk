{#
variables:
  epicId: string        # Epic ID (E001)
  epicTitle: string|null # Epic title
  prdId: string         # Parent PRD ID
  mode: worktree|inline # Execution mode
  sandbox: boolean      # Running in sandbox
  role: coordinator     # Actor role
  epicBody: string      # Epic content
  memory: string|null   # Epic memory (optional)
#}
# Epic Review: {{ epicId }}

{% if epicTitle %}**{{ epicTitle }}**{% endif %}
PRD: {{ prdId }}

---

## Your Role

You are the **coordinator** reviewing this epic.

- ✅ Check technical decisions are documented
- ✅ Verify acceptance criteria are testable
- ✅ Ensure dependencies are realistic
- ❌ DO NOT implement anything

---

## Review Checklist

{% if sandbox %}
```json
{ "command": "epic:show {{ epicId }} --raw" }
{ "command": "task:list --epic {{ epicId }}" }
{ "command": "deps:show {{ epicId }}" }
```
{% else %}
```bash
rudder epic:show {{ epicId }} --raw
rudder task:list --epic {{ epicId }}
rudder deps:show {{ epicId }}
```
{% endif %}

---

## What to Check

### 1. Technical Decisions
- Are tech choices documented?
- Any risky assumptions?
- Integration points clear?

### 2. Acceptance Criteria
- Are criteria testable?
- Any missing edge cases?
- Performance requirements?

### 3. Dependencies
- Are blockers identified?
- External dependencies listed?
- Realistic task ordering?

---

## Recording Findings

{% if sandbox %}
```json
{ "command": "epic:edit {{ epicId }} -s \"## Technical Notes\" -c \"Review findings...\" --append" }
```
{% else %}
```bash
rudder epic:edit {{ epicId }} -s "## Technical Notes" -c "Review findings..." --append
```
{% endif %}

---

## Status Update

After review:
{% if sandbox %}
```json
{ "command": "epic:update {{ epicId }} --status \"In Review\"" }
```
{% else %}
```bash
rudder epic:update {{ epicId }} --status "In Review"
```
{% endif %}

---

# Epic Content

{{ epicBody }}

{% if memory %}
---

# Memory

{{ memory }}
{% endif %}
